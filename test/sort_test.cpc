DECLARE n : INTEGER
n <- 100000
DECLARE arr : ARRAY[1:n] OF REAL

FOR i <- 1 TO n
    arr[i] <- RAND(n)
NEXT i

OUTPUT "FINISH GENERATING ARRAY"

// CALL QuickSort(arr, 1, n)
CALL Sort(arr, 1, n)
OUTPUT arr
