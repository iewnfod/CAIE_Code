PROCEDURE outputName(name: STRING)
    OUTPUT "Name: ", name
ENDPROCEDURE

FUNCTION connect_name(s1 : STRING, s2 : STRING) RETURNS STRING
    DECLARE a : STRING
    a <- s1 & '·' & s2
    CALL outputName(a)
    RETURN a
ENDFUNCTION

DECLARE a : STRING
a <- "hcr"
a <- connect_name(a, "ifsaiojaf")
OUTPUT a
